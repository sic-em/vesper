import { By } from '../components/credits'

## New

- Web players. Some shows never get a cached stream because the debrid service refuses them, Dana White's Contender Series among them. The source picker now lists a handful of web players under the cached streams. Pick one and it plays in the app like anything else, with a seek bar, a switcher if a player is dead, and your Discord status. Cached streams still come first when they exist. <By u="sic-em" pr={56} />

---

Got an idea, or did something break? Tell us about it. Click **Feedback** at the bottom of the sidebar, it goes straight to us.
